# Expense Tracker App

## Setup

```bash
git clone <repo-url>
cd expense_tracker
pip install -r requirements.txt
uvicorn app:app --reload

you can create user in user section by entering emailid
enter expenses in expenses section